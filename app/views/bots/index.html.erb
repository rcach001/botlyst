
<div class="jumbotron">
  <h2> The first comprehensive list of bots to make your life easier!</h2>
  <h3> Find the right bot for you</h3><br>
  <p><a class="btn btn-primary btn-lg" href="#" role="button">Search Bots</a></p>
</div>

<h3>Popular Bots:</h3>
<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bots.each do |bot| %>
      <tr>
        <td><%= link_to bot.name, bot %></td>
        <td><%= bot.description %></td>
        <td><%= link_to bot.website, bot.website %></td>
        <td><%= link_to 'Show', bot, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_bot_path(bot), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', bot, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bot', new_bot_path, class:"btn btn-link" %>
